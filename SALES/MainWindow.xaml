<Window x:Class="SALES.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SALES"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1" Grid.Column="0"/>
        <DataGrid Grid.Row="1" Grid.Column="1"/>
        <DataGrid Grid.Row="1" Grid.Column="2"/>
        <Button Content="Добавить сотрудника" Grid.Row="2" Grid.Column="0" />
        <Button Content="Добавить продукт" Grid.Row="2" Grid.Column="1" />
        <Button Content="Добавить продажу" Grid.Row="2" Grid.Column="2" />
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="3" />
            </Style>
        </Grid.Resources>
    </Grid>
</Window>
